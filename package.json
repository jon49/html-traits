{
  "name": "html-traits",
  "version": "0.0.3",
  "description": "Built-in web components for all elements.",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild --minify --format=iife index.js > index.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "html",
    "web-components",
    "is",
    "built-in",
    "trait",
    "traits"
  ],
  "author": "Jon Nyman",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
